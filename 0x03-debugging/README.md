cd ~/set-low_level_programming/0x03-debugging
cat > README.md << 'EOF'
# 0x03-debugging

This directory contains the testing files for the `positive_or_negative` function:
- `main.h` — header file
- `0-main.c` — tests the `positive_or_negative` function for the 0 case
EOF

