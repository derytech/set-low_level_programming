# 0x00. C - Hello, World

This project introduces basic concepts of C programming and the compilation process using GCC.

## Task 0: Preprocessor

A shell script that runs a C file through the preprocessor and saves the output into a file named `c`.  
The script is named `0-preprocessor` and works on any C file provided in the `$CFILE` variable.

